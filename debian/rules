#!/usr/bin/make -f

# commented becouse dangerous, see
# http://bugs.debian.org/cgi-bin/bugreport.cgi?bug=%23311724
# DEB_AUTO_UPDATE_DEBIAN_CONTROL := yes 
# use fakeroot debian/rules debian/control DEB_AUTO_UPDATE_DEBIAN_CONTROL:=yes

DEB_TAR_SRCDIR := .
DEB_CONFIGURE_EXTRA_FLAGS ?= 

include /usr/share/cdbs/1/rules/utils.mk
include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/class/autotools.mk

pre-build::
	chmod 755 ./autogen.sh
	./autogen.sh

clean::
	$(RM) \
	Makefile.in \
	aclocal.m4 \
	auxdir/Makefile.in \
	auxdir/depcomp \
	auxdir/install-sh \
	auxdir/mdate-sh \
	auxdir/missing \
	auxdir/texinfo.tex \
	config.h.in \
	configure \
	doc/Makefile.in \
	doc/spro.info \
	doc/stamp-vti \
	doc/version.texi \
	test/Makefile.in \
	test/dat/Makefile.in \
	test/ref/Makefile.in
